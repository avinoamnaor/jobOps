# Base URL of the JobOps FastAPI backend.
#
# Vite only exposes variables prefixed with VITE_ to browser code — a guard
# against accidentally shipping a server secret into a JavaScript bundle.
VITE_API_BASE_URL=http://localhost:8000
